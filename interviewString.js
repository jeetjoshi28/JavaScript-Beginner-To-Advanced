//write a javascript function that prints the letter 'a' through 'z' in the console. you should use a loop to iterate through the letter and print each one on a new line

for (let char = 97; char <= 122; char++) {
  console.log(String.fromCharCode(char));
}

// write a function to count the number of vowels in a string

//write a function to check if all the voels present in a string or not?

const checkAllVowelPresentOrNot = (str) => {
  const vowels = "aeiou";
};
